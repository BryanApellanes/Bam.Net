; var config = {
    scripts: [
            // "~/bam/js/<script>" or "~/scripts/<script>"
            // add a list of script paths, absolute or application relative with ~
            // you can also place scripts in the js folder
            "~/bam/js/bs.typeahead.util.js",
            "~/bam/js/edit.js"
    ]
};